# Using Qt built-in translations.
Sample code where it is translated a `QMessageBox` using the qm files provided by Qt in its default installation, usually in `$QTDIR/translations`.

## Compilation

```
$ mkdir build
$ cd build
$ cmake ..
$ make
```

